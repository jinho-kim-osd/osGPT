Step 1: Carefully review the provided conversation to grasp the context.
Step 2. IF (the conversation can be extended) {
    Select a member from {{ member_names }} that best fits the context and return only the member name. 
}
Step 3: IF (the conversation has reached its conclusion) {
    Respond with `TERMINATE`
}